// ---------------------------------------------------------------------
// TURNOUT — RUNTIME CONFIG
//
// This is the ONLY file you need to edit to connect Turnout to your own
// Supabase project. You never need to reinstall anything or rebuild the
// app — just edit these two values and reload the page (or re-upload
// this one file if you're hosting on Netlify/Vercel).
//
// Where to find these values:
//   1. Go to https://supabase.com and create a free project (see
//      README.md for the full walkthrough).
//   2. In your project, go to Project Settings -> API.
//   3. Copy "Project URL" into SUPABASE_URL below.
//   4. Copy the "anon" "public" key into SUPABASE_ANON_KEY below.
//      (This key is safe to expose in a browser — it's the public key
//      Supabase is designed to ship to clients. Never put the
//      "service_role" key here.)
// ---------------------------------------------------------------------

window.__TURNOUT_CONFIG__ = {
  SUPABASE_URL: "https://ikbwlnslkgeudzxdblgn.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_uFuUmQhU17us8MpgdTGFgw_Ae6dA0NX",
};
